# Data directory

Generated training and validation HDF5 files are written to `generated/` and are excluded from Git. See `docs/data.md` for the exact distributions, seeds, sizes, and generation commands. Evaluation scenes are generated on demand and are not stored here.
