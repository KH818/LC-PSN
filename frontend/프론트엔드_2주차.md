# 프론트엔드 2주차 작업 정리

## 1. 작업 목적

이번 주차에는 프론트엔드를 백엔드 WebSocket stream과 연결하고, 수신한 DOA 추론 결과를 대시보드 형태로 시각화했다.

초기 화면은 placeholder와 raw JSON 중심이었기 때문에 실제 시연용 화면으로 보기 어려웠다. 따라서 다음 작업을 진행했다.

- `/predict` 호출 제거
- `/health` 기반 서버 연결 확인
- WebSocket 메시지 수신 및 history 저장
- 추론 이벤트 정규화 함수 추가
- Polar, Spectrum, Waterfall chart 구현
- Summary panel 추가
- Vite 기본 전역 스타일 제거 및 대시보드 레이아웃 정리

## 2. 백엔드 연결 변경

### `frontend/.env`

변경 전에는 프론트에서 백엔드 주소를 직접 사용했다.

```env
VITE_API_BASE_URL=http://127.0.0.1:8000
```

변경 후에는 Vite proxy를 사용한다.

```env
VITE_API_BASE_URL=/api
VITE_WS_URL=ws://127.0.0.1:8000/ws
```

### `frontend/vite.config.js`

`/api` 요청을 백엔드로 proxy하도록 설정했다.

```text
/api/health -> http://127.0.0.1:8000/health
```

이렇게 한 이유는 브라우저에서 직접 `http://127.0.0.1:8000`으로 요청할 때 발생할 수 있는 CORS 문제를 피하기 위해서다.

## 3. 변경한 함수 기준 정리

## 3.1 `useWebSocket(url)`

파일:

```text
frontend/src/hooks/useWebSocket.js
```

역할:

- WebSocket 연결 생성
- 연결 상태 관리
- 최신 메시지 저장
- 최근 메시지 history 저장
- 마지막 메시지 수신 시각 저장
- 연결 에러 저장

반환값:

```js
return {
  status,
  message,
  messageHistory,
  lastMessageAt,
  error,
};
```

주요 상태:

- `status`: `connected`, `disconnected`, `error`
- `message`: 가장 최근 WebSocket 메시지
- `messageHistory`: 최근 WebSocket 메시지 목록
- `lastMessageAt`: 마지막 수신 시간
- `error`: WebSocket 연결 에러

추가한 이유:

Waterfall chart는 한 프레임만으로 그릴 수 없고, 여러 프레임의 spectrum history가 필요하다. 그래서 `messageHistory`를 추가했다.

## 3.2 `normalizeInferenceEvent(event)`

파일:

```text
frontend/src/utils/inferenceEvent.js
```

역할:

백엔드에서 오는 이벤트 구조를 프론트엔드 차트가 쓰기 쉬운 공통 구조로 변환한다.

현재 백엔드 mock event:

```json
{
  "type": "stream_inference_result",
  "data": {
    "estimated_k": 5,
    "doas_deg": [-9, -1, 28, 45, 48],
    "confidence": 0.93,
    "spectrum": []
  }
}
```

향후 문서 기준 정식 event:

```json
{
  "event_id": "...",
  "k_estimate": 3,
  "doa_deg": [-31.5, 4.0, 42.5],
  "peak_scores": [0.94, 0.88, 0.76],
  "spectrum": {
    "grid_start": -90,
    "grid_end": 90,
    "grid_size": 360,
    "values": []
  }
}
```

정규화 후 프론트에서 사용하는 구조:

```js
{
  eventId,
  type,
  timestamp,
  sensorId,
  kEstimate,
  doas,
  peakScores,
  confidence,
  spectrum,
  sampleIndex,
  inputFile,
  streaming,
}
```

추가한 이유:

백엔드 mock 필드명은 `estimated_k`, `doas_deg`, `spectrum`이고, 문서의 정식 구조는 `k_estimate`, `doa_deg`, `spectrum.values`이다. 차트 컴포넌트가 이 차이를 직접 알게 하면 유지보수가 어려워지므로 중간 정규화 함수를 만들었다.

## 3.3 `getPayload(event)`

파일:

```text
frontend/src/utils/inferenceEvent.js
```

역할:

이벤트에서 실제 payload 부분을 꺼낸다.

현재 mock 서버는 다음처럼 `data` 안에 실제 값이 들어온다.

```js
event.data
```

하지만 향후에는 event 자체가 payload가 될 수 있으므로 두 경우를 모두 처리한다.

## 3.4 `getSpectrum(payload)`

파일:

```text
frontend/src/utils/inferenceEvent.js
```

역할:

spectrum 데이터를 공통 구조로 변환한다.

현재 mock 형식:

```js
payload.spectrum = [0.1, 0.2, 0.3]
```

정식 이벤트 형식:

```js
payload.spectrum = {
  grid_start: -90,
  grid_end: 90,
  grid_size: 360,
  values: []
}
```

프론트 공통 형식:

```js
{
  values,
  gridStart,
  gridEnd,
  gridSize,
}
```

## 3.5 `MainLayout`

파일:

```text
frontend/src/component/layout/MainLayout.jsx
```

역할:

전체 대시보드 화면을 구성한다.

변경 전:

- `message?.data`를 각 chart에 직접 전달
- raw JSON을 `Latest Data` 카드로 출력

변경 후:

- `normalizeInferenceEvent(message)`로 이벤트 정규화
- 정규화된 `inference` 객체를 chart와 summary panel에 전달
- raw JSON 카드 제거
- Summary panel 추가

현재 구성:

```text
Header
Sidebar
InferenceSummaryPanel
PolarChart
SpectrumChart
WaterfallChart
```

## 3.6 `InferenceSummaryPanel`

파일:

```text
frontend/src/component/panels/InferenceSummaryPanel.jsx
```

역할:

가장 중요한 추론 결과를 한눈에 볼 수 있게 요약한다.

표시 항목:

- K estimate
- Confidence
- DOA angles
- Spectrum bins

추가한 이유:

기존에는 raw JSON을 그대로 보여줘서 화면이 복잡했다. 시연 화면에서는 필요한 값만 빠르게 보여주는 요약 패널이 더 적합하다.

## 3.7 `PolarChart`

파일:

```text
frontend/src/component/charts/PolarChart.jsx
```

역할:

DOA 각도를 반원형 polar chart로 표시한다.

주요 내부 함수:

```js
toPoint(angleDeg, distance)
```

역할:

- -90도부터 +90도 사이의 DOA 값을 SVG 좌표로 변환한다.
- 변환된 좌표를 이용해 중심점에서 DOA 방향으로 ray를 그린다.

개선 사항:

- DOA source 개수가 3개 이하일 때만 각 ray 옆에 라벨을 표시한다.
- source가 많을 때는 라벨이 겹치므로 chart header에 각도 목록을 표시한다.

## 3.8 `SpectrumChart`

파일:

```text
frontend/src/component/charts/SpectrumChart.jsx
```

역할:

spectrum 배열을 line chart로 표시한다.

주요 처리:

- `normalizeNumberList`로 숫자 배열 정리
- `getRange`로 y축 범위 계산
- `scaleValue`로 score 값을 SVG y좌표로 변환
- `points` 문자열을 만들어 SVG `polyline`으로 출력

문서 기준으로는 `grid_start`, `grid_end`, `grid_size`를 이용해 angle grid를 복원할 수 있도록 준비했다.

## 3.9 `WaterfallChart`

파일:

```text
frontend/src/component/charts/WaterfallChart.jsx
```

역할:

최근 WebSocket 메시지들의 spectrum history를 heatmap 형태로 표시한다.

주요 처리:

- `messageHistory`에서 spectrum만 추출
- 최근 36개 frame만 사용
- 각 spectrum 값을 색상으로 변환
- CSS grid를 이용해 heatmap cell로 표시

주요 내부 함수:

```js
getHeatColor(value)
```

역할:

- spectrum 값을 0~1 비율로 정규화
- 값이 낮으면 차가운 색, 값이 높으면 따뜻한 색으로 표시

## 3.10 `ConnectionStatusPanel`

파일:

```text
frontend/src/component/panels/ConnectionStatusPanel.jsx
```

역할:

서버 연결 상태를 보여준다.

표시 항목:

- API URL
- WebSocket URL
- 연결 상태
- 수신한 메시지 수
- 마지막 메시지 수신 시각

추가한 이유:

WebSocket 연결은 되었지만 stream message가 오지 않는 상황을 구분하기 위해 `Messages`와 `Last received`를 추가했다.

## 3.11 `handleStart`

파일:

```text
frontend/src/App.jsx
```

역할:

`Check Server` 버튼을 눌렀을 때 백엔드 `/health` endpoint를 확인한다.

변경 전:

```text
POST /predict
```

변경 후:

```text
GET /api/health
```

변경 이유:

최신 백엔드에서는 `/predict` endpoint가 제거되었고, 추론 결과는 WebSocket stream으로 자동 수신되는 구조가 되었기 때문이다.

## 4. 스타일 변경

## 4.1 `index.css`

Vite 기본 템플릿 스타일을 제거했다.

제거한 문제 요소:

- `#root` 고정 폭
- 전체 `text-align: center`
- 전역 `h1` 대형 스타일
- 불필요한 dark mode 변수

## 4.2 `App.css`

대시보드 전용 스타일로 재작성했다.

주요 구성:

- header
- sidebar
- summary panel
- chart card
- responsive grid
- polar chart style
- spectrum chart style
- waterfall heatmap style

## 5. 백엔드 확인 내용

백엔드 WebSocket endpoint는 정상 동작했다.

```text
ping -> pong
```

하지만 처음에는 stream message가 오지 않았다. 원인은 백엔드 stream이 다음 파일에 의존하기 때문이었다.

```python
STREAM_H5_PATH = "train_mini_1bit.h5"
```

필요한 파일 위치:

```text
LC_PSN/train_mini_1bit.h5
```

파일을 추가한 뒤에는 백엔드 서버를 재시작해야 한다.

정상 실행 로그:

```text
[STREAM] Loaded train_mini_1bit.h5
[STREAM] X shape = ...
[STREAM] Sent sample index 0
```

## 6. 주요 변경 파일

```text
frontend/.env
frontend/vite.config.js
frontend/src/App.jsx
frontend/src/App.css
frontend/src/index.css
frontend/src/hooks/useWebSocket.js
frontend/src/utils/inferenceEvent.js
frontend/src/component/layout/Header.jsx
frontend/src/component/layout/MainLayout.jsx
frontend/src/component/layout/Sidebar.jsx
frontend/src/component/panels/ConnectionStatusPanel.jsx
frontend/src/component/panels/ControlPanel.jsx
frontend/src/component/panels/InferenceSummaryPanel.jsx
frontend/src/component/charts/PolarChart.jsx
frontend/src/component/charts/SpectrumChart.jsx
frontend/src/component/charts/WaterfallChart.jsx
```

## 7. 실행 방법

백엔드:

```powershell
cd C:\Users\MOK\Desktop\Kwangwoon\졸작\LC-PSN\LC_PSN
python -m uvicorn server.main:app --reload --host 127.0.0.1 --port 8000
```

프론트엔드:

```powershell
cd C:\Users\MOK\Desktop\Kwangwoon\졸작\LC-PSN\frontend
npm.cmd run dev -- --host 127.0.0.1
```

접속 주소:

```text
http://127.0.0.1:5173
```

## 8. 검증

다음 명령어를 통과했다.

```powershell
npm.cmd run lint
npm.cmd run build
```

## 9. 커밋

프론트엔드 대시보드 개선 커밋:

```text
29cd3bc Improve realtime DOA dashboard UI
```

