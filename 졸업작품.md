# 딥러닝 기반 1-bit MIMO 표적 방향 탐지(DOA) 소프트웨어 개발 제안서
### 차세대 레이더 대체 지능형 신호처리 시스템

---

**제안 기관:** [연구기관명]  
**제안 일자:** 2026년 4월  
**보안 등급:** 대외비  
**문서 번호:** [문서번호]

---
## 개발 인원
팀장: 권혁    - 모델 개발
팀원: 전한아솔 -모델 API 개발
팀원: 김경목  - 실시간 데이터 파이프라인 개발
팀원: 조영인  - 결과 저장/DB 개발 



## 목차

1. 제안 배경 및 필요성
2. 기존 레이더 시스템의 구조적 한계
3. 1-bit 양자화 기반 수신 시스템의 우위
4. 공간 스펙트럼 기반 DOA 추정의 기술적 우위
5. 제안 시스템 아키텍처 및 핵심 기술
6. 기대 효과
7. 개발 로드맵
8. 결론 및 제언

---

## I. 제안 배경 및 필요성

### 1.1 현대 전장 환경의 변화

현대전은 드론 군집, 극초음속 미사일, 전자전(EW) 무기체계의 동시 복합 위협으로 빠르게 재편되고 있다. 이러한 환경에서 탐지·추적 시스템에 요구되는 핵심 역량은 세 가지로 수렴한다.

**첫째, 다중 표적의 동시 탐지 능력이다.** 단일 표적을 상정한 기존 레이더 체계는 군집 드론이나 복수의 적 발사체가 동시 진입하는 상황에서 근본적 한계를 드러낸다. 특히 신호원 수(K)가 사전에 알려지지 않은 상황에서의 동적 탐지는 전통적 알고리즘이 해결하지 못하는 영역이다.

**둘째, 전자기 은닉성(Low Probability of Intercept, LPI)이다.** 현대 적 전자전 장비는 아군 레이더의 고출력 방사 신호를 역으로 탐지·역추적하는 수준에 도달했다. 강력한 송신 신호를 기반으로 하는 기존 능동 레이더는 오히려 아군 플랫폼의 위치를 노출시키는 위험 요인이 되고 있다.

**셋째, 극한 환경에서의 경량·저전력 운용 능력이다.** UAV 탑재, 소형 함정 통합, 분산 감시망 구성 등 미래 전장의 다양한 플랫폼은 기존 대형 레이더 시스템의 물리적 조건(크기, 무게, 전력)을 수용할 수 없다.

### 1.2 제안의 핵심 주장

본 제안서는 **딥러닝 기반 1-bit MIMO 표적 방향 탐지(Direction of Arrival, DOA) 소프트웨어**가 위의 세 가지 요구를 동시에 충족하는 실현 가능한 기술적 해법임을 논증한다.

제안 시스템의 핵심은 두 가지 기술적 선택에 있다.

- **1-bit 아날로그-디지털 변환(ADC):** 수신 신호의 진폭 정보 대신 부호(±1)만을 기록함으로써 ADC 하드웨어 비용과 전력을 수십 분의 일로 절감하면서도, Van-Vleck 아크사인 보정을 통해 원본 통계량을 복원한다.
- **Spectrum-only 딥러닝 추정기:** 공간 스펙트럼 전체를 출력 표적으로 삼는 Transformer 기반 모델이 신호원 수를 사전에 알 필요 없이 복수의 DOA를 동시 추정한다.

---

## II. 기존 레이더 시스템의 구조적 한계

### 2.1 하드웨어 아키텍처의 병목

현재 운용 중인 능동 위상 배열 레이더(APAR) 및 기계식 레이더 시스템은 공통적으로 다음의 구조적 제약을 안고 있다.

#### 2.1.1 고해상도 ADC의 비용·전력 문제

위상 배열 레이더에서 M개의 안테나 소자 각각에 고해상도 ADC(12~16 bit)를 부착하는 것은 현대 다중 안테나 시스템의 표준 구성이다. 그러나 M이 수십~수백 개로 확장되는 대형 MIMO 배열에서는 ADC 개수의 선형 증가가 곧 시스템 전체 비용 및 전력 소모의 지배적 요인이 된다.

구체적으로, 최신 군용 위상 배열 레이더에 쓰이는 16-bit ADC 1개의 전력 소모는 약 100~500 mW 수준이다. 안테나 소자 64개를 갖춘 시스템에서는 ADC 부분만으로도 6~32 W의 전력이 소모되며, 이는 소형 무인 플랫폼의 전체 전력 예산을 초과하는 경우가 많다.

1-bit ADC의 전력 소모는 동일 샘플링 레이트 기준 약 1~5 mW로, **100배 이상의 전력 절감**이 가능하다.

#### 2.1.2 ADC 비선형성 및 보정 부담

고해상도 ADC는 제조 공정 편차로 인해 각 소자마다 상이한 DC 오프셋, 이득 불일치, 비선형 왜곡을 가진다. 이를 보정하기 위한 정기적 교정(calibration) 과정은 운용 가용도(availability)를 저하시킨다. 반면 1-bit ADC는 부호만을 기록하므로 보정 복잡도가 원천적으로 낮다.

#### 2.1.3 고출력 송신기의 전자기 노출 문제

기존 능동 레이더는 표적 반사 신호의 극히 낮은 SNR을 극복하기 위해 킬로와트급 송신 전력을 사용한다. 이 고출력 전자기 방사는 다음의 전략적 취약점을 만들어낸다.

- **역추적 가능성:** 적 ESM(Electronic Support Measures) 장비가 방사 신호를 포착해 아군 레이더 위치를 역추적
- **재밍 취약성:** 방사 주파수와 패턴이 알려지면 집중 재밍으로 무력화 가능
- **스텔스 플랫폼 탑재 불가:** UAV, 잠수함, 특수전 차량 등 은닉성 요구 플랫폼에 탑재 불가

### 2.2 신호처리 알고리즘의 한계

#### 2.2.1 고전적 MUSIC 알고리즘의 K 의존성

현재 가장 널리 쓰이는 부공간 기반 DOA 추정 알고리즘인 MUSIC은 신호원 수 K를 사전에 알아야 한다. K 추정을 위한 정보이론적 기준(AIC, MDL) 방법들은 저SNR 환경이나 신호가 공간적으로 근접한 경우 추정 오류율이 크게 높아진다. K가 1이라도 잘못 추정되면 알고리즘 전체가 오작동한다.

실전 환경에서 신호원 수는 사전에 알 수 없다. 드론 군집의 경우 수 대에서 수십 대가 동시에 진입할 수 있으며, 이 숫자는 실시간으로 변한다.

#### 2.2.2 상관 신호에 대한 취약성

다중경로 전파 환경(복잡한 지형, 해상)에서 동일 표적으로부터 직접파와 반사파가 동시에 수신될 때, 두 신호는 강한 상관성을 가진다. 전통적인 MUSIC은 이 경우 공분산 행렬이 계수 부족(rank deficient) 상태가 되어 올바른 신호 부공간을 추출하지 못한다. Spatial Smoothing 등의 보정 기법이 존재하지만 배열 길이 손실과 계산 복잡도 증가를 수반한다.

#### 2.2.3 실시간 처리의 계산 복잡도

M×M 공분산 행렬의 EVD(Eigenvalue Decomposition) 복잡도는 O(M³)이다. 안테나 수 M=64의 경우 행렬 크기가 64×64이며, 이를 실시간으로(예: 1 kHz 업데이트 레이트) 처리하기 위해서는 전용 DSP 또는 FPGA 자원이 필요하다. 반면 딥러닝 추론은 GPU 또는 NPU에서 병렬화되어 유사한 레이턴시를 훨씬 저렴한 하드웨어로 달성할 수 있다.

### 2.3 한계 요약

| 항목 | 기존 레이더 시스템 | 본 제안 시스템 |
|---|---|---|
| ADC 해상도 | 12~16 bit (소자당) | 1 bit (소자당) |
| ADC 전력 (소자당) | 100~500 mW | 1~5 mW |
| 전체 ADC 전력 (M=64) | 6~32 W | 0.06~0.32 W |
| 신호원 수 K 사전 지식 | 필수 | 불필요 |
| 상관 신호 처리 | 추가 보정 필요 | 학습으로 내재화 |
| 전자기 방사 | 고출력 능동 방사 | 수동 수신(passive) |
| 플랫폼 탑재 다양성 | 대형 플랫폼 한정 | 소형 UAV까지 확장 |

---

## III. 1-bit 양자화 기반 MIMO 수신 시스템의 우위

### 3.1 1-bit 양자화의 물리적 원리

1-bit ADC는 아날로그 입력 신호의 부호(sign)만을 기록한다. 즉 입력 신호 x(t)에 대해 z(t) = sign(Re{x(t)}) + j·sign(Im{x(t)})로 양자화한다. 각 샘플은 {+1+j, +1-j, -1+j, -1-j} 네 가지 값 중 하나만 취하며, 진폭 정보는 완전히 손실된다.

이로 인해 공분산 행렬 추정이 왜곡되는 것은 사실이다. 그러나 **Van-Vleck/Bussgang 정리**는 이 왜곡에 수학적 구조가 있음을 보여준다.

영평균 복소 가우시안 신호 x가 1-bit 양자화된 신호 z의 상관계수와 원본 신호의 상관계수 사이에는 다음의 아크사인 법칙이 성립한다:

```
ρ_원본 ≈ sin( (π/2) · ρ_양자화 )
```

이 역변환을 공분산 행렬의 각 원소에 적용하면 원본 신호의 공분산 행렬을 추정할 수 있다. 본 제안 시스템에서는 이 수학적 보정을 딥러닝 모델 내부에 명시적으로 구현하고(`onebit_arcsine_cov`), Transformer가 그 위에 데이터 기반 보정을 추가로 학습한다.

### 3.2 하드웨어 이점 -> 면적 및 비용, 발열 및 방열 시스템 단순화, 데이터 전송 대역폭 절감

#### 3.2.1 면적 및 비용

1-bit ADC는 단순 비교기(comparator) 하나로 구현 가능하다. 고해상도 SAR-ADC나 파이프라인 ADC와 비교할 때 면적은 수백 분의 일, 비용은 수십 분의 일 수준이다. MIMO 배열에서 소자 수가 수십~수백으로 늘어날 때, 이 차이는 시스템 전체 비용에 결정적인 영향을 미친다.

#### 3.2.2 샘플링 레이트 확장

1-bit ADC는 고해상도 ADC보다 훨씬 높은 샘플링 레이트 달성이 용이하다. 이는 광대역 신호 처리와 높은 시간 해상도 확보에 유리하다.

#### 3.2.3 발열 및 방열 시스템 단순화

고해상도 ADC 어레이는 고밀도 전력 소모로 인해 방열 설계가 복잡하다. 1-bit 시스템은 발열이 극히 낮아 소형 밀폐형 플랫폼(수중 부이, 소형 UAV)에서도 별도 방열 없이 운용 가능하다.

### 3.3 신호처리 이점

#### 3.3.1 정보이론적 관점

Shannon 정보이론 관점에서 1-bit 양자화는 큰 정보 손실처럼 보인다. 그러나 DOA 추정에서 진정으로 필요한 정보는 신호의 정확한 진폭이 아니라 안테나 소자 간의 위상 관계(공분산 구조)이다. 스냅샷 수 T가 충분히 크면 1-bit 관측에서도 위상 구조를 통계적으로 복원할 수 있다는 것이 이론적으로 증명되어 있다.

#### 3.3.2 SNR과의 관계

흥미롭게도, 저SNR 환경에서 1-bit 양자화의 성능 저하는 고SNR 환경보다 작다. 노이즈가 많은 환경에서는 진폭 정보 자체의 신뢰도가 낮기 때문에 부호 정보의 상대적 가치가 더 높아지기 때문이다. 실제로 본 제안 모델의 훈련 데이터는 SNR 0~10 dB 조건이며, 이 범위에서 SNR 0 dB의 최악 조건에서도 DOA 추정 RMSE가 1.15도 이내를 달성했다.

#### 3.3.3 데이터 전송 대역폭 절감

MIMO 수신 어레이에서 디지털 처리 유닛으로 전송되는 데이터량이 크게 줄어든다. M=64, T=200 스냅샷 기준으로 16-bit 시스템 대비 데이터 전송량이 16분의 1로 감소하며, 이는 분산 센서 네트워크 구성 시 통신 병목을 대폭 완화한다.

---

## IV. 공간 스펙트럼 기반 DOA 추정의 기술적 우위

### 4.1 기존 딥러닝 DOA 추정 연구의 한계

딥러닝 기반 DOA 추정 연구는 크게 두 흐름으로 나뉜다.

**회귀(Regression) 방식**은 신경망이 DOA 각도값을 직접 출력한다. 이 방식은 신호원 수 K가 고정되어야 하며, K가 달라지면 출력 차원이 달라져 재훈련이 필요하다. 실전 환경처럼 K가 동적으로 변하는 상황에는 적용이 불가능하다.

**분류(Classification) 방식**은 각도 그리드를 이진 분류 문제로 다룬다. K가 변해도 출력 차원이 고정되는 장점이 있지만, BCE 손실 함수가 양수·음수 그리드 간의 극심한 불균형(K:G-K ≈ 4:356)으로 인해 학습이 불안정해지는 문제가 있다. 또한 대부분의 연구는 훈련 데이터에 포함된 K 범위를 넘어서면 성능이 급락한다.

**모델 기반 딥러닝 방식(DNN-aided inference)**은 MUSIC과 같은 물리 알고리즘의 일부 단계를 신경망으로 대체한다. 그러나 SVD를 역전파 경로에 포함하면 그래디언트 불안정 문제가 발생하고, K 추정을 위한 별도 분류기를 추가 훈련해야 하는 복잡성이 있다.

### 4.2 Spectrum-only 접근법의 핵심 차별점

본 제안 시스템(TransSpectrum)이 채택한 Spectrum-only 방식은 이들과 근본적으로 다른 설계 철학을 가진다.

#### 4.2.1 연속적 공간 스펙트럼을 직접 학습

모델은 G=360개 각도 그리드 각각에 대해 "이 각도에 신호원이 있을 확률"을 [0,1] 범위의 연속값으로 출력한다. 이 **스펙트럼 자체**가 모델의 학습 목표이다. DOA는 이 스펙트럼에서 피크를 검출함으로써 후처리 단계에서 추출되므로, K가 몇 개든 같은 모델이 처리할 수 있다.

기존의 분류 방식이 {0, 1} 이진 레이블을 목표로 삼는 것과 달리, 본 방식은 실제 DOA 위치에 **가우시안 소프트 타겟**을 배치한다. 이는 각도 그리드의 이산화로 인한 학습 신호 손실을 줄이고, 훈련 초기에 모델이 올바른 방향을 학습하도록 돕는 커리큘럼 학습 효과를 준다 (σ: 2.0° → 1.2°).

#### 4.2.2 물리 모델과 데이터 기반 모델의 이중 잔차 구조

제안 시스템은 Bartlett 스펙트럼(물리 기반)과 MLP 보정(데이터 기반)을 잔차 덧셈으로 결합한다:

```
spec_logits = spec_residual + β · logit(base_spec)
```

`β`(초기값 0.75)는 학습되는 파라미터로, 물리 prior에 얼마나 의존할지를 데이터로부터 자동 결정한다. 이 구조는 학습 데이터가 부족한 초기에는 물리 모델에 의존하고, 학습이 진행될수록 데이터 기반 보정을 강화하는 자연스러운 정규화 효과를 제공한다.

#### 4.2.3 공분산 행렬의 Transformer 정제

CovarianceTokenizer는 공분산 행렬의 각 행(row)을 하나의 토큰으로 변환한다. 이 M개 토큰이 Transformer Encoder를 통과하면서 안테나 소자 간 상관 구조를 self-attention으로 학습한다. 이후 학습된 잔차 행렬 Δ가 초기 공분산에 더해져 정제된 공분산 R_ref를 만든다:

```
R_ref = R0 + α · Δ
```

`α`(초기값 0.10)는 학습되는 파라미터로, 정제 강도를 자동 조절한다. 이 구조는 SVD를 역전파 경로에서 완전히 제거함으로써 기존 모델 기반 딥러닝의 그래디언트 불안정 문제를 해결한다.

### 4.3 기존 연구 대비 성능 비교 -> 기존의 모델인 TransMUSIC의 결과 사진과 현재 모델의 사진을 같이 첨부할 것(K-acc)

| 방법 | K 사전 지식 | 변동 K 처리 | 1-bit 신호 | SVD 역전파 | 물리 prior 통합 |
|---|---|---|---|---|---|
| 고전 MUSIC | 필수 | 추정기 별도 필요 | 불가 | — | 완전 |
| 회귀 기반 DL | 필수 | 불가 | 가능 | 없음 | 없음 |
| 분류 기반 DL (BCE) | 불필요 | 훈련 범위 내 한정 | 가능 | 없음 | 없음 |
| DA-MUSIC | 별도 분류기 필요 | 훈련 범위 내 한정 | 불가 | 있음 | 부분 |
| Trans-MUSIC | 별도 분류기 필요 | 훈련 범위 내 한정 | 가능 | 없음 | 부분 |
| **TransSpectrum (제안)** | **불필요** | **훈련 범위 초과 가능** | **설계 목적** | **없음** | **이중 잔차** |

### 4.4 시뮬레이션 성능 결과

아래 결과는 M=8 ULA, T=200 스냅샷, G=360 각도 그리드 조건에서 SNR 0~10 dB 혼합 훈련 후 SNR별 평가를 수행한 결과이다.



(K-correct 조건 기준, 신호원 수 K=2~5 혼합 평가)

이 결과는 기존 1-bit MUSIC 알고리즘 대비 다음의 개선을 보여준다.

- SNR 0 dB에서 DOA RMSE: 기존 1-bit MUSIC ~4.5° → 본 시스템 ~1.15° (약 **4배 개선**)
- K 추정 없이 동적 신호원 수 처리: 기존 방법 대비 결정적 우위
- 실시간 추론 속도: GPU 기준 배치 128개를 약 5 ms 이내 처리 -> 애는 아직 목표로 남길 것
- end point에서 ULA의 특징으로 인한 overshooting 현상이 문제 -> 처리해야 할 목표
---

## V. 제안 시스템 아키텍처 및 핵심 기술

### 5.1 전체 시스템 구성-> 내가 준 HTML 파일의 사진을 첨부할 것

Layer 1 — 실시간 데이터 파이프라인: ZeroMQ
Kafka와 Redis Streams는 둘 다 훌륭하지만 브로커 서버를 별도로 띄워야 합니다. Kafka는 특히 Zookeeper나 KRaft 클러스터까지 필요해서 운용 환경이 복잡해집니다. DOA 추정 시스템은 데이터 소스가 안테나 하나이고 소비자도 추론 서버 하나인 단순한 1:1 구조입니다. ZeroMQ는 브로커 없이 라이브러리만 import하면 되고, 지연 시간이 마이크로초 단위로 셋 중 가장 낮습니다. 실시간 신호처리에서 레이턴시가 핵심인 만큼 인프라 복잡도를 줄이면서 속도를 얻는 ZeroMQ가 이 규모에 가장 맞습니다.

Layer 2 — 모델 추론 서버: FastAPI
TorchServe와 ONNX Runtime은 대규모 프로덕션 환경에 최적화된 도구입니다. 하지만 이 시스템은 모델이 하나이고, decode_spectrum_peaks의 threshold 같은 파라미터를 런타임에 조정하는 커스텀 로직이 추론 결과와 함께 처리돼야 합니다. TorchServe는 이런 커스텀 후처리를 끼워 넣으려면 핸들러 코드를 별도로 작성해야 해서 오히려 번거롭습니다. FastAPI는 Python 함수 하나가 곧 엔드포인트가 되고, 기존 모델 코드를 거의 그대로 가져다 쓸 수 있습니다. WebSocket 엔드포인트도 같은 프레임워크에서 처리할 수 있어서 Layer 4 백엔드와 통합도 자연스럽습니다.

Layer 3 — 데이터베이스: InfluxDB (시계열), PostgreSQL (트랙)
두 가지가 필요한 이유가 있어서 하나만 고르기보다는 역할을 나눠서 선택했습니다. DOA 추정 결과는 "50ms마다 타임스탬프와 함께 쌓이는 수치 데이터"로 전형적인 시계열 패턴이고, InfluxDB는 이 패턴에 특화되어 "최근 10분간 30도 방향 신호 평균"같은 쿼리가 PostgreSQL보다 훨씬 빠릅니다. 반면 표적 궤적은 트랙 ID, 출현·소멸 시각, 각도 이력이 서로 관계를 맺는 구조여서 관계형 DB가 맞습니다. PostgreSQL은 별도 설치 없이 대부분의 서버에서 바로 쓸 수 있고, SQLite는 운용 규모가 작을 때 파일 하나로 시작할 수 있어서 초기 개발에 유리합니다.

Layer 4 — 백엔드 비즈니스 로직: 칼만 필터 (Kalman Filter)
Hungarian 알고리즘은 프레임 간 DOA를 매칭하는 역할이고, 칼만 필터는 그 매칭된 궤적을 스무딩하는 역할이라 사실 두 가지가 짝을 이룹니다. 그중 칼만 필터를 강조하는 이유는 DOA 추정에 필연적으로 노이즈가 있기 때문입니다. 모델이 프레임마다 30.2°, 29.8°, 30.5°를 출력하면 화면에서 마커가 계속 흔들립니다. 칼만 필터는 이전 상태와 현재 측정값을 가중 평균해서 30.1°, 30.0°, 30.1°처럼 안정적인 궤적을 만들고, 측정이 잠깐 빠져도 예측값으로 표적을 유지합니다. scipy에 이미 구현이 있어서 수십 줄로 바로 붙일 수 있습니다.

Layer 5 — UI: D3.js
React와 WebGL은 각각 컴포넌트 관리와 고성능 렌더링에 강하지만, 레이더 화면의 핵심인 극좌표 시각화는 D3.js가 독보적입니다. D3는 SVG 기반으로 각도 스케일, 방사형 축, 호(arc), 피크 마커를 수식 그대로 코드로 표현할 수 있고, 스펙트럼 360개 값을 원형 바 차트로 그리는 것이 20줄 안에 가능합니다. WebSocket으로 들어오는 실시간 데이터를 .data() 바인딩으로 연결하면 DOM 업데이트도 자동 처리됩니다. React는 D3와 함께 쓰되 레이아웃과 상태 관리 담당으로 역할을 분리하는 것이 실제 구현에서 가장 깔끔한 조합입니다.

## VI. 기대 효과



### 6.2 운용 시나리오별 적용 가능성 -> 1,2개만 갔다가 사용할 것(1개는 UAV 탑재 가능하다로)

#### 6.2.1 지상 방공 시스템

다수의 드론 또는 미사일이 동시 진입하는 포화 공격(saturation attack) 시나리오에서, 기존 레이더는 K 추정 오류로 인해 탐지 실패 확률이 증가한다. 본 시스템은 K 사전 지식 없이 최대 5개(소프트웨어 파라미터로 확장 가능)의 동시 표적을 탐지할 수 있다.

#### 6.2.2 함정 전자전 지원

함정 탑재 전자전 시스템에서 적 레이더 방사원의 DOA를 추정하는 수동 탐지(passive detection) 임무에 적합하다. 1-bit 수신으로 아군 전자기 방사가 없어 피탐 위험이 없다.

#### 6.2.3 소형 UAV 탑재 감시

저전력(ADC 어레이 < 1 W), 경량(소프트웨어 처리), 소형 구성으로 전술 UAV 탑재가 가능하다. 지속적인 표적 방위 감시 및 포병 사격 제원 제공에 활용 가능하다.

#### 6.2.4 잠수함 수동 소나 확장

수중 음향 신호의 DOA 추정에도 동일한 수학적 프레임워크가 적용 가능하다. 음향 신호를 전기 신호로 처리하는 방식은 동일하며, ULA 대신 수중 선배열 센서(towed array)를 사용하는 경우에도 소프트웨어 파라미터 조정으로 대응 가능하다.

### 6.3 기대 효과 요약

**기술적 효과:**
- 미래 전장 핵심 요구(저전력·경량·은닉성·다중 표적 동시 탐지)를 단일 시스템으로 충족
- 기존 레이더 시스템 대비 ADC 전력 소모 100분의 1 달성 가능
- 신호원 수 미지(unknown K) 상황에서의 실용적 DOA 추정 최초 구현

**전략적 효과:**
- 수동 수신 방식으로 아군 전자기 노출 최소화 → 생존성 향상
- 소형 플랫폼(UAV, 부이, 특수전 장비)으로 감시망 분산화 가능
- 소프트웨어 기반 설계로 하드웨어 교체 없이 알고리즘 업데이트 가능

**경제적 효과:**
- ADC 하드웨어 단가 절감으로 대규모 배열 시스템의 획득 비용 대폭 절감
- 소프트웨어 정의 구조(Software-Defined)로 후속 유지보수 비용 절감
- 국내 원천기술 확보로 기술 수입 대체 및 수출 가능성

---

## VII. 개발 로드맵 
일정 
1~3월 데이터 셋 탐색 및 논문 스터디
4~5월 모델 개발
5~6월 모델 추론API 및 실시간 데이터 파이프 라인
7~9월 UI/UX 대시보드 개발
10~11월 최종 보고서 및 논문


향후 시간이 남으면 표적 트랙킹 로직을 위해 Kalman filter를 적용하여 프레임간 DOA를 연결해 궤적 추적할 예정

---

## VIII. FastAPI 추론 데이터 포맷 설계

### 8.1 기본 원칙

FastAPI 서버는 LC-PSN 모델의 내부 출력 텐서를 그대로 프론트엔드에 노출하지 않고, 추론 결과를 프론트엔드와 저장소가 사용하기 쉬운 JSON 이벤트로 변환한다.

현재 LC-PSN 모델의 출력은 다음과 같은 내부 추론용 구조이다.

```python
{
    "mu": spectrum_mean,        # [B, G], 각도 grid별 spectrum score
    "sigma2": spectrum_var,     # [B, G], 보조 variance head
    "mu_logits": mu_logits,     # [B, G]
    "log_var": log_var,         # [B, G]
    "K_logits": K_logits,       # [B, K_classes]
    "R_ref": R_ref,             # [B, M, M], refined covariance
    "S_base": S_base,           # [B, G], Bartlett prior spectrum
    "R0": R0,                   # [B, M, M], arcsine covariance
    "U0": U0,                   # [B, 2M, 2M], finite-snapshot uncertainty
    "tau_hat": tau_hat          # [B, 2M, 2M]
}
```

이 구조는 학습, 평가, 진단에는 유용하지만 API 응답으로는 너무 크고 내부 구현에 종속적이다. 따라서 모델 output은 유지하고, FastAPI 응답 직전에 adapter를 두어 다음 정보를 추출한다.

```text
K_hat = argmax(K_logits) + K_min
score(theta) = mu(theta)
DOA_hat = mu에서 top-K_hat separated peaks
spectrum = mu 전체 또는 downsampled/압축된 값
```

### 8.2 timestamp 정책

실시간 시스템에서는 timestamp를 하나만 두지 않고 다음 세 값을 분리한다.

```text
sensor_timestamp      센서 또는 수집 모듈에서 신호가 생성된 시각
server_received_at    FastAPI 서버가 데이터를 받은 시각
inference_finished_at 모델 추론과 후처리가 끝난 시각
```

센서 시간이 없는 경우에는 `server_received_at`을 기준 시간으로 사용한다. InfluxDB에는 보통 `inference_finished_at` 또는 `server_received_at`을 time index로 저장하고, 원본 센서 시간이 존재하면 별도 field로 함께 저장한다.

### 8.3 raw data 저장 방식

raw one-bit snapshot 전체를 매번 WebSocket으로 프론트엔드에 보내지 않는다. 실시간 UI에는 raw 전체가 필요하지 않고, 매 프레임 raw를 보내면 네트워크 부하와 브라우저 메모리 사용량이 커진다.

권장 방식은 다음과 같다.

```text
1. FastAPI가 raw snapshot을 받는다.
2. 서버가 raw_data_id를 생성한다.
3. raw snapshot은 파일, HDF5, DB blob, 또는 object storage에 저장한다.
4. 프론트엔드에는 raw_data_id와 추론 결과만 전송한다.
5. 디버깅이 필요할 때만 GET /raw/{raw_data_id}로 raw를 조회한다.
```

raw data 형식은 현재 모델 입력과 맞춰 다음을 기본으로 한다.

```text
format: real_augmented_onebit
shape: [2M, T]
dtype: int8 또는 float32
layout: [Re_1, ..., Re_M, Im_1, ..., Im_M] x T
values: {-1, +1}
```

### 8.4 FastAPI/WebSocket 응답 JSON 예시

프론트엔드와 백엔드에 전달할 한 프레임 추론 결과는 다음 구조를 권장한다.

```json
{
  "event_id": "20260513T153012.123Z_000042",
  "sensor_id": "ula_01",
  "sensor_timestamp": "2026-05-13T06:30:12.100Z",
  "server_received_at": "2026-05-13T06:30:12.123Z",
  "inference_finished_at": "2026-05-13T06:30:12.129Z",
  "input": {
    "raw_data_id": "raw_20260513_000042",
    "format": "real_augmented_onebit",
    "shape": [16, 200],
    "m": 8,
    "snapshots": 200,
    "dtype": "int8",
    "layout": "[Re_1..Re_M, Im_1..Im_M] x T",
    "values": "not_included"
  },
  "model": {
    "name": "LC-PSN",
    "checkpoint": "best_lcpsn_1bit.pt",
    "angles_count": 360,
    "k_min": 2,
    "k_max": 5,
    "min_separation_deg": 8.0
  },
  "output": {
    "k_estimate": 3,
    "k_confidence": 0.91,
    "doa_deg": [-31.5, 4.0, 42.5],
    "doa_rad": [-0.5498, 0.0698, 0.7418],
    "peak_scores": [0.94, 0.88, 0.76],
    "spectrum": {
      "grid_unit": "deg",
      "grid_start": -90.0,
      "grid_end": 90.0,
      "values": [0.01, 0.02, 0.03]
    }
  },
  "diagnostics": {
    "latency_ms": 6.2,
    "spectrum_max": 0.94,
    "spectrum_mean": 0.07,
    "peak_count_015": 5,
    "peak_count_030": 3
  }
}
```

실제 `spectrum.values`는 `angles_count=360`이면 길이 360 배열이다. 프론트엔드가 원형 스펙트럼이나 line chart를 그릴 때 이 값을 그대로 사용한다.

### 8.5 Pydantic 스키마 초안

FastAPI에서는 다음과 같은 Pydantic 모델을 사용한다.

```python
from datetime import datetime
from typing import Literal
from pydantic import BaseModel


class InputMeta(BaseModel):
    raw_data_id: str
    format: Literal["real_augmented_onebit"]
    shape: list[int]
    m: int
    snapshots: int
    dtype: Literal["int8", "float32"]
    layout: str
    values: str = "not_included"


class ModelMeta(BaseModel):
    name: str
    checkpoint: str
    angles_count: int
    k_min: int
    k_max: int
    min_separation_deg: float


class SpectrumOutput(BaseModel):
    grid_unit: Literal["deg"]
    grid_start: float
    grid_end: float
    values: list[float]


class InferenceOutput(BaseModel):
    k_estimate: int
    k_confidence: float | None = None
    doa_deg: list[float]
    doa_rad: list[float]
    peak_scores: list[float]
    spectrum: SpectrumOutput


class DOAInferenceEvent(BaseModel):
    event_id: str
    sensor_id: str
    sensor_timestamp: datetime | None = None
    server_received_at: datetime
    inference_finished_at: datetime
    input: InputMeta
    model: ModelMeta
    output: InferenceOutput
    diagnostics: dict
```

### 8.6 LC-PSN output을 바꿔야 하는가?

현재 LC-PSN 모델의 output 구조는 바꾸지 않는 것이 좋다. 이유는 다음과 같다.

```text
1. train.py, eval.py, diagnose_lcpsn_spectrum.py가 현재 output key에 의존한다.
2. mu, sigma2, R0, U0, R_ref 등은 실험과 진단에 필요하다.
3. API 응답 형식은 서비스 계층의 문제이고, 모델 forward 형식과 분리하는 편이 유지보수에 안전하다.
```

따라서 구조는 다음처럼 나눈다.

```text
LC-PSN forward output
-> API adapter
-> DOAInferenceEvent JSON
-> WebSocket/HTTP response
```

모델 내부 출력은 그대로 유지하고, FastAPI 서버에서 `format_lcpsn_response(...)` 같은 함수를 만들어 `mu`, `K_logits`, raw metadata, timestamp를 조합해 API 응답을 생성한다.

### 8.7 저장소별 저장 항목

InfluxDB에는 빠른 시계열 조회에 필요한 scalar 중심 값을 저장한다.

```text
time
sensor_id
k_estimate
doa_1_deg, doa_2_deg, ...
peak_score_1, peak_score_2, ...
spectrum_max
spectrum_mean
latency_ms
raw_data_id
checkpoint
```

raw snapshot과 전체 spectrum은 크기가 크므로 별도 저장소나 파일로 저장한다.

```text
raw snapshot: raw_data_id 기준 .npy/.h5/object storage
full spectrum: 필요하면 event_id 기준 JSON/NPY 저장
track 정보: PostgreSQL 또는 SQLite
```

프론트엔드는 WebSocket으로 `DOAInferenceEvent`를 받아 최신 DOA 마커와 spectrum plot을 갱신하고, 과거 raw data가 필요할 때만 REST API로 조회한다.
---

## IX. FastAPI Output Adapter 구현 계획

### 9.1 실시간 전달 경로와 DB 저장 경로

FastAPI 서버가 반드시 DB를 거쳐서 프론트엔드에 결과를 보낼 필요는 없다. 실시간 화면 갱신은 FastAPI가 WebSocket으로 프론트엔드에 직접 push하는 것이 더 적절하다. DB는 실시간 전달 경로가 아니라 기록, 조회, 복구, 분석을 위한 저장소로 둔다.

권장 구조는 다음과 같다.

```text
실시간 표시:
FastAPI inference server
-> WebSocket
-> Frontend

저장/분석/복구:
FastAPI inference server
-> InfluxDB / PostgreSQL / raw file storage

과거 조회:
Frontend
-> FastAPI REST API
-> DB 또는 raw storage
```

DB를 중간 전달 경로로 사용하지 않는 이유는 다음과 같다.

```text
1. DB polling 방식은 WebSocket direct push보다 latency가 크다.
2. 프론트엔드가 일정 주기로 DB를 조회하면 불필요한 query 부하가 생긴다.
3. DB 장애가 실시간 화면 표시 장애로 직접 이어질 수 있다.
4. 현재 프레임 표시에는 저장소 조회가 필요 없고, FastAPI가 이미 추론 결과를 가지고 있다.
```

DB가 필요한 이유는 별도이다.

```text
1. 최근 10분, 특정 시간 구간, 특정 sensor_id 결과 조회
2. raw_data_id 기반 재현성 확보
3. 프론트엔드 연결 끊김 이후 누락 구간 복구
4. Kalman filter 또는 track history 구성
5. latency, K 변화, spectrum 통계, 오탐 패턴 분석
6. 이상 결과 발생 시 raw snapshot 재확인
```

따라서 구현에서는 FastAPI 추론 함수가 결과를 만든 직후 두 작업을 분기한다.

```text
1. WebSocket manager로 DOAInferenceEvent 즉시 broadcast
2. background task로 InfluxDB/raw storage에 비동기 저장
```

### 9.2 LC-PSN output adapter 설계

LC-PSN 모델은 API 응답을 직접 만들지 않는다. 모델은 내부 텐서를 반환하고, FastAPI의 adapter가 이 텐서를 서비스용 JSON으로 변환한다.

현재 LC-PSN output과 API output의 대응 관계는 다음과 같다.

```text
raw data:
  모델 output에는 없음.
  FastAPI request input x를 raw storage에 저장하고 raw_data_id를 생성한다.

timestamp:
  모델 output에는 없음.
  FastAPI server_received_at, inference_finished_at에서 생성한다.

K estimate:
  모델 output에는 K_logits만 있음.
  k_hat = argmax(K_logits) + K_min 으로 변환한다.

K confidence:
  모델 output에는 직접 없음.
  softmax(K_logits).max()를 사용한다.

DOA 위치:
  모델 output에는 직접 없음.
  mu에서 top-K_hat separated peaks를 선택한다.

spectrum:
  모델 output의 mu를 사용한다.
  프론트엔드는 angle grid와 mu values로 spectrum plot을 그린다.

peak score:
  선택된 DOA peak index에서 mu 값을 읽어 만든다.
```

adapter 함수의 역할은 다음과 같다.

```python
def format_lcpsn_response(
    *,
    out,
    x,
    angle_grid_rad,
    raw_data_id,
    sensor_id,
    checkpoint_name,
    server_received_at,
    inference_finished_at,
    k_min=2,
    k_max=5,
    min_separation_deg=8.0,
):
    """
    LC-PSN internal output을 FastAPI/WebSocket용 DOAInferenceEvent dict로 변환한다.
    """
```

구체적인 처리 절차는 다음과 같다.

```text
1. mu = out["mu"][0]
2. k_prob = softmax(out["K_logits"][0])
3. k_hat = argmax(k_prob) + k_min
4. selected_idx = top-K_hat separated peak indices from mu
5. doa_rad = angle_grid_rad[selected_idx]
6. doa_deg = rad2deg(doa_rad)
7. peak_scores = mu[selected_idx]
8. spectrum.values = mu.tolist()
9. input metadata와 timestamp를 조합해 DOAInferenceEvent를 만든다.
```

### 9.3 adapter 코드 예시

아래 코드는 FastAPI endpoint 내부 또는 별도 `api_adapter.py`에서 사용할 수 있는 형태의 예시이다.

```python
import math
import uuid
from datetime import datetime, timezone

import torch


def select_topk_separated_indices(score, angle_grid_rad, k_hat, min_separation_deg=8.0):
    score_cpu = score.detach().cpu()
    grid_cpu = angle_grid_rad.detach().cpu()
    min_sep = math.radians(min_separation_deg)

    candidates = []
    for i in range(1, score_cpu.numel() - 1):
        if score_cpu[i] > score_cpu[i - 1] and score_cpu[i] > score_cpu[i + 1]:
            candidates.append(i)

    if not candidates:
        candidates = torch.argsort(score_cpu, descending=True).tolist()

    selected = []
    for idx in sorted(candidates, key=lambda j: float(score_cpu[j]), reverse=True):
        if all(abs(float(grid_cpu[idx] - grid_cpu[j])) >= min_sep for j in selected):
            selected.append(idx)
        if len(selected) >= k_hat:
            break

    return sorted(selected)


def format_lcpsn_response(
    out,
    x,
    angle_grid_rad,
    raw_data_id,
    sensor_id,
    checkpoint_name,
    server_received_at,
    inference_finished_at,
    k_min=2,
    k_max=5,
    min_separation_deg=8.0,
):
    mu = out["mu"][0].detach().cpu()
    k_logits = out["K_logits"][0].detach().cpu()
    k_prob = torch.softmax(k_logits, dim=-1)
    k_hat = int(torch.argmax(k_prob).item()) + k_min
    k_hat = max(k_min, min(k_max, k_hat))

    selected_idx = select_topk_separated_indices(
        mu,
        angle_grid_rad.detach().cpu(),
        k_hat,
        min_separation_deg=min_separation_deg,
    )

    doa_rad = [float(angle_grid_rad[i].detach().cpu().item()) for i in selected_idx]
    doa_deg = [float(math.degrees(v)) for v in doa_rad]
    peak_scores = [float(mu[i].item()) for i in selected_idx]

    event = {
        "event_id": f"{datetime.now(timezone.utc).isoformat()}_{uuid.uuid4().hex[:8]}", #추론 결과 하나를 구분하는 고유 ID, 나중에 DB, raw data, 로그, 프론트 화면에서 같은 프레임을 추적하려면 필요
        "sensor_id": sensor_id, #어느 안테나/수신기에서 온 데이터인지 나타냄
        "sensor_timestamp": None, #하드웨어나 ZeroMQ 송신부에서 시간을 줄 수 있으면 넣고, 없으면 None으로 둬도 됨
        "server_received_at": server_received_at.isoformat(), #FastAPI 서버가 데이터를 받은 시간, 실시간 시스템에서 “입력 지연”이나 “서버 수신 순서”를 확인할 때 사용
        "inference_finished_at": inference_finished_at.isoformat(), #모델 추론과 후처리가 끝난 시간, server_received_at과 비교하면 전체 처리 latency를 계산가능
        "input": { #모델에 들어간 데이터가 어떤 데이터였는지 설명하는 metadata
            "raw_data_id": raw_data_id, #원본 raw snapshot을 찾기 위한 ID, 프론트에 raw 전체를 매번 보내면 너무 무겁기 때문에, raw는 서버에 저장하고 ID만 보내자
            "format": "real_augmented_onebit", #입력 표현 방식. 현재 LC-PSN은 복소수 신호를 real/imag로 나눈 one-bit sign input을 사용하므로 real_augmented_onebit.
            "shape": list(x.shape[-2:]), #입력 tensor의 shape. M=8, T=200이면 [16, 200] 앞의 16은 2M임
            "m": int(x.shape[-2] // 2), #실제 안테나 개수
            "snapshots": int(x.shape[-1]), #시간 snapshot 개수 T
            "dtype": str(x.dtype).replace("torch.", ""), #데이터 타입. API 저장/디버깅에서 필요
            "layout": "[Re_1..Re_M, Im_1..Im_M] x T", #[Re_1..Re_M, Im_1..Im_M] x T 순서라는 뜻 이걸 명시해야 프론트나 다른 서버가 raw를 다시 읽을 때 혼동하지 않는다
            "values": "not_included", #raw data를 event 안에 포함하지 않는다는 표시. 필요하면 별도 /raw/{raw_data_id} API로 조회하게 하는 게 좋음
        },
        "model": { #어떤 모델과 어떤 설정으로 결과가 나왔는지 설명
            "name": "LC-PSN",
            "checkpoint": checkpoint_name, #사용한 weight 파일, 나중에 결과 재현이나 버전 비교에 중요
            "angles_count": int(mu.numel()), #spectrum grid 개수. 현재 보통 360
            "k_min": k_min, #K classifier가 예측하는 source 수 범위 현재는 보통 2~5
            "k_max": k_max,
            "min_separation_deg": min_separation_deg, #DOA peak를 뽑을 때 최소 간격, 예를 들어 8도면, 두 peak가 8도보다 가까우면 같은 source 후보로 보고 하나만 선택
        },
        "output": {
            "k_estimate": k_hat, #추정된 신호원 개수. 모델의 K_logits에서 argmax를 해서 얻음
            "k_confidence": float(k_prob.max().item()), #K 추정의 confidence. softmax(K_logits).max()로 계산. 예를 들어 0.91이면 모델이 예측한 K에 대해 꽤 확신
            "doa_deg": doa_deg, #추정된 DOA 각도입니다. 프론트에서는 이 값을 바로 marker 위치로 사용
            "doa_rad": doa_rad, #같은 값을 radian으로 표현한 것입니다. 백엔드나 수학 계산에서는 radian이 편하므로 같이 넣어놓음
            "peak_scores": peak_scores, #각 DOA peak 위치에서의 spectrum score입니다. marker 크기, 색상, confidence 표시 등에 사용할 수 있다
            "spectrum": {
                "grid_unit": "deg", #각도 단위
                "grid_start": -90.0,
                "grid_end": 90.0,
                "grid_size": int(mu.numel()), #각 index가 몇 도인지 복원하기 위한 정보
                "values": [float(v) for v in mu.tolist()], #길이 360짜리 mu 값입니다. 프론트는 이걸 line chart나 polar chart로 그려야 함
            },
        },
        "diagnostics": { #성능 모니터링과 디버깅용
            "latency_ms": ( #서버가 데이터를 받은 시점부터 추론 완료까지 걸린 시간
                inference_finished_at - server_received_at
            ).total_seconds() * 1000.0,
            "spectrum_max": float(mu.max().item()), #spectrum의 최대값. 값이 너무 낮으면 모델이 확신이 약하다는 신호일 수 있음
            "spectrum_mean": float(mu.mean().item()), #전체 spectrum 평균 너무 높으면 spectrum이 전체적으로 퍼져 있어서 peak가 불명확할 수 있음
        },
    }
    return event
```
FAST API에서 내부적으로 full event를 만들고, WebSocket으로 보낼 때는 필요한 필드만 골라서 lightweight event로 보내는 방식을 사용

1. WebSocket 실시간 메시지
  작고 빠르게:

  {
    "event_id": "...",
    "timestamp": "...",
    "sensor_id": "ula_01",
    "k_estimate": 3,
    "doa_deg": [-31.5, 4.0, 42.5],
    "peak_scores": [0.94, 0.88, 0.76],
    "spectrum": {
      "grid_start": -90,
      "grid_end": 90,
      "grid_size": 360,
      "values": [...]
    }
  }

  2. DB/log 저장용 full event
  나중에 재현과 분석을 위해 자세하게:

  {
    "event_id": "...",
    "sensor_id": "...",
    "sensor_timestamp": "...",
    "server_received_at": "...",
    "inference_finished_at": "...",
    "input": {...},
    "model": {...},
    "output": {...},
    "diagnostics": {...}
  }

### 9.4 FastAPI endpoint 흐름 예시

FastAPI endpoint에서는 다음 순서로 처리한다.

```python
from datetime import datetime, timezone
from fastapi import FastAPI, BackgroundTasks

app = FastAPI()


@app.post("/infer")
async def infer(request, background_tasks: BackgroundTasks):
    server_received_at = datetime.now(timezone.utc)

    x = parse_request_to_tensor(request)
    raw_data_id = save_raw_snapshot(x)

    with torch.no_grad():
        out = model(x, a_steering)

    inference_finished_at = datetime.now(timezone.utc)

    event = format_lcpsn_response(
        out=out,
        x=x,
        angle_grid_rad=angle_grid_rad,
        raw_data_id=raw_data_id,
        sensor_id=request.sensor_id,
        checkpoint_name=CHECKPOINT_NAME,
        server_received_at=server_received_at,
        inference_finished_at=inference_finished_at,
    )

    await websocket_manager.broadcast(event)
    background_tasks.add_task(write_event_to_db, event)

    return event
```

이 구조에서는 프론트엔드가 현재 프레임을 보기 위해 DB를 기다리지 않는다. DB 저장은 실패하더라도 실시간 표시와 분리할 수 있고, 저장 실패는 별도 로그와 재시도 큐로 처리한다.

### 9.5 프론트엔드에서 spectrum을 그리는 방법

프론트엔드는 `output.spectrum.values`를 y값으로 사용하고, angle grid를 x값으로 복원한다.

```javascript
const spectrum = event.output.spectrum;
const values = spectrum.values;

const points = values.map((score, i) => {
  const angle =
    spectrum.grid_start +
    i * (spectrum.grid_end - spectrum.grid_start) / (spectrum.grid_size - 1);

  return { angle, score };
});
```

DOA 마커는 프론트에서 다시 peak detection하지 않고, 백엔드가 보낸 `output.doa_deg`를 사용한다. 이렇게 해야 백엔드 평가 결과와 프론트 표시가 달라지는 문제를 막을 수 있다.

```text
spectrum.values -> spectrum line 또는 polar chart
output.doa_deg  -> DOA marker
peak_scores     -> marker confidence 또는 marker 크기
k_estimate      -> 현재 탐지된 신호원 수 표시
```
